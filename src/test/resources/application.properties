# We can have a separate application.properties file for testing
# This file will be used when we run the tests
# We can have a separate database for testing
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=sa
spring.datasource.password=password